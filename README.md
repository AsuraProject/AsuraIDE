Clone the <a href="https://github.com/electron/electron-quick-start">electron-quick-starter</a> and copy the node_modules to AsuraIDE folder
and install dependencies with 'npm install' and start with 'npm start'
